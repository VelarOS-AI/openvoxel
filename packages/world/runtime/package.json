{
  "name": "@openvoxel/world-runtime",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "files": [
    "src"
  ],
  "velar": {
    "entry": "src/index.vel",
    "targets": ["core"],
    "requires": {
      "capabilities": []
    }
  },
  "scripts": {
    "benchmark": "velar run benchmarks/active-world-state.vel",
    "check": "velar check",
    "format": "velar format",
    "format:check": "velar format --check",
    "test": "velar test",
    "build": "velar build"
  },
  "dependencies": {
    "@openvoxel/blocks": "0.2.0",
    "@openvoxel/content": "0.2.0",
    "@openvoxel/world": "0.2.0",
    "@openvoxel/world-generation": "0.2.0"
  },
  "devDependencies": {
    "@openvoxel/identities": "0.2.0"
  }
}
