import {BlockMutation} from "./http.vel"

export type SocketBlockCommand extends BlockMutation:
    operation: string

export type BlockChangedEvent extends BlockMutation:
    event: string
    previousBlockStateRuntimeId: number
    revision: number

export type SocketErrorEvent:
    event: string
    code: string
    message: string
