{
  "name": "@openvoxel/hash",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "files": [
    "src"
  ],
  "velar": {
    "entry": "src/index.vel",
    "targets": [
      "core",
      "node",
      "web",
      "desktop"
    ],
    "requires": {
      "capabilities": []
    }
  },
  "scripts": {
    "check": "velar check",
    "format": "velar format",
    "format:check": "velar format --check",
    "test": "velar test",
    "build": "velar build"
  },
  "devDependencies": {
    "@velarscript/cli": "0.22.0"
  }
}
