{
  "name": "@openvoxel/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "src"
  ],
  "velar": {
    "entry": "src/index.vel",
    "targets": [
      "core",
      "node",
      "web",
      "desktop"
    ],
    "requires": {
      "capabilities": []
    }
  },
  "scripts": {
    "check": "velar check",
    "format": "velar format",
    "format:check": "velar format --check",
    "test": "velar test",
    "build": "velar build",
    "benchmark": "velar run benchmarks/world-generation.vel",
    "benchmark:caves": "velar run benchmarks/caves.vel",
    "benchmark:columns": "velar run benchmarks/columns.vel"
  },
  "dependencies": {
    "@openvoxel/blocks": "0.1.0",
    "@velarscript-labs/noise": "0.1.1"
  },
  "devDependencies": {
    "fast-check": "4.9.0"
  }
}
